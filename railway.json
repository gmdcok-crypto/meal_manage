{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "preDeployCommand": ["python", "repair_db.py"],
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
